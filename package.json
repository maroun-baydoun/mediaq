{
  "name": "mediaq-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter mediaq build",
    "demo": "pnpm --filter mediaq-demo dev",
    "test": "pnpm --filter mediaq build && pnpm --filter mediaq-test test",
    "format": "prettier --write './{apps,packages}/**/*.{ts,html,json}' './*.{json,md,yaml,yml}'"
  },
  "devDependencies": {
    "prettier": "catalog:"
  },
  "engines": {
    "node": "24",
    "pnpm": "11"
  }
}
